/** Shared cadence keeps browser polling and the server-side Open-Meteo cache aligned. */
export const WEATHER_REFRESH_MINUTES = 30
export const WEATHER_REFRESH_SECONDS = WEATHER_REFRESH_MINUTES * 60
export const WEATHER_REFRESH_MS = WEATHER_REFRESH_SECONDS * 1000
